import { styles } from "../../styles/styles";

const NotFound = () => {
   return (
      <div className={`${styles.h2} text-center my-10`}>
         error 404 page is not found(
      </div>
   );
};

export default NotFound;
